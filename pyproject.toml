[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codra"
version = "0.1.0"
description = "AST-based static analysis tool"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["codra"]
